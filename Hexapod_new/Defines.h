#ifndef DEFINES_H
#define DEFINES_H

enum MoveStatus
{
    MOVE_STATUS_STOPPED,
    MOVE_STATUS_MOVING
};

enum Pulses
{
    DEFAULT_MIN_MICROS          = 550,
    DEFAULT_MAX_MICROS          = 2400
};

#endif